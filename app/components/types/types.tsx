export interface Images {
  Imageurl: string;
}
export interface NavigationLinks {
  name: string;
  href: string;
}
export interface ThirdImage {
  title: string;
  icon: React.ReactNode;
  photo: string;
  content: string;
}

export interface ForthImage {
  avatar: string;
  name: string;
  custumer: string;
  message: string;
}
